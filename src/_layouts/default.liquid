<!doctype html>
<html lang="{{ site.locale }}">
  <head>
    {% render 'head', metadata: site.metadata, title: data.title %}
  </head>
  <body>
        <!-- Page content here -->
        <div class="md:container md:max-w-4xl md:mx-auto px-0 sm:px-2">
          <main class="min-h-screen mt-4 px-3">
            {{ content }}
          </main>
        </div>
      </div>
  </body>
</html>
