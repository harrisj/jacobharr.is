<!doctype html>
<html lang="{{ site.locale }}">
  <head>
    {% render 'head', metadata: site.metadata, title: data.title %}
  </head>
  <body>
        <!-- Page content here -->
        <div class="@container max-w-lg md:max-w-xl lg:max-w-2xl mx-auto">
          <main class="min-h-screen mt-4 px-3">
            {{ content }}
          </main>
        </div>
      </div>
  </body>
</html>
