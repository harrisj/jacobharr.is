<!doctype html>
<html lang="{{ site.locale }}">
  <head>
    <%= liquid_render 'head', metadata: site.metadata, title: data.title %>
  </head>
  <body>
    <div class="">
        <%= yield %>
    </div>
  </body>
</html>
