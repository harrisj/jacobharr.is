<!doctype html>
<html lang="{{ site.locale }}">
  <head>
    <%= liquid_render 'head', metadata: site.metadata, title: data.title %>
  </head>
  <body>
    <main>
        <%= yield %>
    </main>
  </body>
</html>
