<!doctype html>
<html lang="en-US">
  <head>
    <%= liquid_render 'head', metadata: site.metadata, title: data.title %>
  </head>
  <body>
    <main>
        <%= yield %>
    </main>
  </body>
</html>
